import { TipoFechaPipe } from './tipo-fecha.pipe';

describe('TipoFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
