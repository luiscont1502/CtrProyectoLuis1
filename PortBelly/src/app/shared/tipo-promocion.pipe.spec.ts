import { TipoPromocionPipe } from './tipo-promocion.pipe';

describe('TipoPromocionPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPromocionPipe();
    expect(pipe).toBeTruthy();
  });
});
